# Database Schema â€” sip_edge

> **Auto-generated** by `python .opencode/scripts/schema_dump.py`.
> Este archivo refleja el estado *real* de la base de datos, no el codigo.
> **NO EDITAR A MANO.** Si este archivo contiene solo este mensaje,
> ejecuta el script de dump primero o escribe la estructura manualmente
> si el proyecto no usa BD SQL.

---

No schema yet. Run `python .opencode/scripts/schema_dump.py` after creating
your first database or table. For non-SQL persistence (JSON, files, etc.),
replace this file with a manual description of the data structures.
